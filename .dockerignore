# 자격증명 및 민감 파일 — 절대 이미지에 포함 금지
*.yaml
*.yml
*.env
.env*
kis_devlp.yaml

# 예외: screener 파이프라인 기본 설정(가중치·임계값) — 자격증명 없음, 이미지에 포함 필요
!screener/config/settings.yaml

# 개발/테스트 전용
tests/
.venv/
htmlcov/
.pytest_cache/
.coverage
*.pyc
__pycache__/
*.egg-info/
dist/
build/

# 대시보드 (별도 컨테이너로 분리)
dashboard/

# Git / IDE
.git/
.gitignore
.github/
.vscode/
.idea/

# 문서
# README.md는 제외하지 않는다 — pyproject.toml의 readme 필드를 hatchling이
# 빌드 시 참조하므로, 제외하면 "uv sync --frozen --no-dev"가 실패한다.
claudedocs/
specs/
PROJECT_INDEX.md
LICENSE

# 설정 (이미지 빌드 시 불필요)
.claude/
pyproject.toml.bak
